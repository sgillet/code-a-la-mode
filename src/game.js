/**
 * [NumberToRoman description]
 */
class Game {
  /**
   * Cook game.
   * @param  {int} number Decimal number.
   * @return {string} The roman-formatted number.
   */
  static run(input) {
    return input;
  }
}

module.exports = Game;
